spring:
  application:
    name: mentors-interface
  profiles:
    include:
      - image-key
      - persistence-database
      - persistence-redis
      - jwt

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB